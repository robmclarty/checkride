{
  "name": "checkride-example-dal-boundaries-declarative",
  "version": "0.0.0",
  "private": true,
  "description": "The same DAL policy as dal-boundaries, expressed entirely in fallow zones and ast-grep rules — no custom script.",
  "type": "module",
  "scripts": {
    "check": "checkride"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "@ast-grep/cli": "0.45.0",
    "checkride": "link:../..",
    "fallow": "3.9.1",
    "typescript": "6.0.3"
  }
}
