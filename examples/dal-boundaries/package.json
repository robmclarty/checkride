{
  "name": "checkride-example-dal-boundaries",
  "version": "0.0.0",
  "private": true,
  "description": "A Drizzle/Postgres data-access layer where only a domain's single writer may write its tables, enforced as a build error.",
  "type": "module",
  "scripts": {
    "check": "checkride"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "checkride": "link:../..",
    "fallow": "3.5.0",
    "typescript": "6.0.3"
  }
}
