{
  "name": "checkride-example-shared-preset",
  "version": "0.0.0",
  "private": true,
  "description": "One versioned preset package carrying org-wide policy; each repo extends it and runs only the subset that applies.",
  "type": "module",
  "scripts": {
    "check": "checkride"
  },
  "devDependencies": {
    "@acme/checkride-preset": "link:./preset",
    "checkride": "link:../..",
    "oxlint": "1.74.0",
    "typescript": "6.0.3"
  }
}
