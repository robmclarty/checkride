{
  "name": "checkride-example-existing-repo-baseline",
  "version": "0.0.0",
  "private": true,
  "description": "Adopting checkride on a repo that already has findings: grandfather today's debt, fail on anything new, ratchet down as it gets fixed.",
  "type": "module",
  "scripts": {
    "check": "checkride"
  },
  "devDependencies": {
    "checkride": "link:../..",
    "oxlint": "1.74.0",
    "typescript": "6.0.3"
  }
}
