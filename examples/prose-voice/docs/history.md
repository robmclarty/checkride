# Quota service: rollout history

This page predates the prose gate and reads like it. Every finding below is
grandfathered in [`checkride.baseline.json`](../checkride.baseline.json), which
is what keeps the canonical run green — the gate holds *new* prose to the house
style while the backlog shrinks at its own pace.

In today's ever-evolving landscape, the quota service delivers seamless
onboarding for every team.

It's not just a rate limiter — it's a platform for fairness. We leveraged the
learnings from the pilot, e.g. burst traffic at month boundaries.

There is a dashboard that tracks adoption.
