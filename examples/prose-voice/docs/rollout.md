# Rolling out the gate

Adopt the slot the way this example does: scaffold the style, opt the slot in,
and capture what today's docs already get wrong. New prose is held to the
rules from the first run; the dashboard for the pilot shows the backlog
shrinking as old findings get fixed and pruned.
