{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/robmclarty/checkride/main/schema/checkride.summary.schema.json",
  "title": "checkride run summary",
  "description": "Schema for .check/summary.json — the aggregate report every run writes. A public API for agents and gate consumers. Fields are additive under schema_version 1: they are added (in lockstep with this schema), never renamed, removed, or retyped without a version bump. See docs/contract.md.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "timestamp", "ok", "checks_run", "total_duration_ms", "checks"],
  "properties": {
    "schema_version": {
      "const": 1,
      "description": "Version of this report shape. Bumped only for a breaking (non-additive) change."
    },
    "timestamp": {
      "type": "string",
      "description": "ISO 8601 time the report was written."
    },
    "ok": {
      "type": "boolean",
      "description": "True when every check passed (skipped checks count as ok). Can be vacuous — see checks_run."
    },
    "checks_run": {
      "type": "integer",
      "minimum": 0,
      "description": "Count of checks that actually executed (skipped entries excluded). ok=true with checks_run=0 is a vacuous green: nothing was verified. Gate consumers should treat it as a failure (or run with --strict, which exits 2)."
    },
    "total_duration_ms": {
      "type": "number",
      "minimum": 0,
      "description": "Sum of all check durations in milliseconds."
    },
    "checks": {
      "type": "array",
      "items": { "$ref": "#/definitions/check" },
      "description": "One entry per selected slot, in pipeline order."
    }
  },
  "definitions": {
    "check": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "adapter", "description", "ok", "exit_code", "duration_ms", "output_file"],
      "properties": {
        "name": { "type": "string", "description": "Slot name (e.g. lint), or a custom check's key." },
        "adapter": { "type": ["string", "null"], "description": "Adapter that ran (e.g. oxlint), or null when the slot resolved to nothing." },
        "description": { "type": "string", "description": "Human description of the check." },
        "ok": { "type": "boolean", "description": "Pass/fail after any baseline masking; skipped checks are ok." },
        "skipped": { "type": "boolean", "description": "Present (true) when the check did not execute; see reason." },
        "reason": { "type": "string", "description": "Why a skipped check sat out (no tool detected, disabled in config, unavailable under this package manager, ...)." },
        "exit_code": { "type": ["integer", "null"], "description": "The tool's exit code; -1 for spawn failure or timeout; null when skipped." },
        "duration_ms": { "type": "number", "description": "Wall-clock duration in milliseconds (0 when skipped)." },
        "output_file": { "type": ["string", "null"], "description": "The .check/ file holding the tool's raw JSON output, when it emits JSON on stdout." },
        "baselined": { "type": "integer", "minimum": 1, "description": "Present only when a committed baseline masked diagnostics: how many current findings are grandfathered." }
      }
    }
  }
}
